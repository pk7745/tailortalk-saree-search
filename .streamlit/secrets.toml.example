# Copy this file to .streamlit/secrets.toml for local runs (git-ignored),
# or paste the same key/value into the "Secrets" box in Streamlit Community
# Cloud's app settings when deploying.

GOOGLE_API_KEY = "paste-your-free-gemini-key-here"
